[project]
name = "speaker"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "groq>=1.6.0",
    "huggingface-hub>=0.20.0",
    "langdetect>=1.0.9",
    "numpy>=2.4.6",
    "onnxruntime>=1.27.0",
    "python-dotenv>=1.2.2",
]

[project.optional-dependencies]
# Install Metal llama.cpp separately (see scripts/install_metal.sh):
#   CMAKE_ARGS="-DGGML_METAL=on" uv pip install llama-cpp-python --no-cache
metal = []
